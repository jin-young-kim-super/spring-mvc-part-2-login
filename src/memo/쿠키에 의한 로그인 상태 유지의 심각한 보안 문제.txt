1. 브라우저에서 임의로 쿠키를 변경하여, 다른 유저인 척 로그인이 가능하다. 
2. 해커가 탈취 가능하다. 

결론 : 
고로, 되도록 중요한 정보는 쿠키에 보관하면 안되며, 토큰(랜덤값)을 쿠키값으로 세팅을 하여 다른 유저의 쿠키값을 예측할 수 없게 만들어 부정 로그인을 방지한다. 
그리고 쿠키값에 만료시간을 설정하여 영속 쿠키로 바꾼다. 그래야지 해커가 한 번 쿠키값을 탈취를 해도 계속 해서 사용을 하지 못한다.
위 개념이 바로 [서버 세션]의 개념이다.
다음 시간에는 서버 세션에 대해 알아 보겠따.


